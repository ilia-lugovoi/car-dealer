SELECT
	CAST(id as int) as id,
	CAST(client_id as varchar(100)) as client_id,
	domain,
	city,
	region,
	browser,
	device_category,
	CAST(date as date) as date,
	CAST(pageviews as int) as pageviews,
	campaign,
	keyword,
	medium,
	source,
	CAST(sessions as int) as sessions,
	goal_completion_location,
	CAST(conversion as int) as conversion,
	source_conv,
	CAST(ad_cost as decimal(18, 2)) as ad_cost,
	CAST(sale as int) as sale,
	model_ga_name,
	CAST(model_ga_id as int) as model_ga_id
FROM {{ source('raw', 'ga_sessions') }}
